import styled from '@emotion/styled';

const Container = styled.div`
  padding: 100px 20px;
  text-align: center;
  font-size: 24px;
  font-weight: bold;
`;

const DetailPage = () => {
    return (
        <Container>
            상세페이지
        </Container>
    );
};

export default DetailPage;
